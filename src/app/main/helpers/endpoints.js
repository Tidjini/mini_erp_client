const PRODUCTION = "9000";
const VM_PRODUCTION = "8010";
const DEVELOPMENT = "8000";
const hostname = "dev-pc.promag-groupe.local";

const port = "8000";

// const address = `http://${hostname}`;
const address = `http://10.0.0.20`;

export const URL = `${address}:${port}`;
export const DATA_SERVICE_URL = `${address}:${port}/data_service/`;
